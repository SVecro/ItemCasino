# Gradle and IDE output
build/
.gradle/
out/
.idea/
*.iml
.vscode/

# Local runs (worlds, logs, configs of the dev client and the game-test server)
run/

# Results written by the .bat scripts
build-errors.txt
build-status.txt
gametest-out.txt
gametest-status.txt
release-out.txt
release-status.txt

# Working files of the offline toolkit and of Claude sessions
.offline/
Claude outputs/

# Release jars, copied from build/libs by hand (each is tagged in git)
releases/
